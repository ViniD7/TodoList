import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    containerCheckBox: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    textCheckBox: {
        color: 'white',
    },
    checkBox: {
        marginRight: 10,
    },
    text: {
        color: '#000'
    },
    textSub: {
        color: 'gray'
    }
});

export default styles;
